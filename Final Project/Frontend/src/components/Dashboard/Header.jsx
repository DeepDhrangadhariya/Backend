import React from 'react';
import './Header.css'; // Optional: Include your CSS for styling

const Header = () => {
  return (
    <header className="header">
      <div className="header-title">
        <h1>Login</h1> {/* Centered "Admin" title */}
      </div>
    </header>
  );
};

export default Header;
